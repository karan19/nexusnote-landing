# NexusNote — Scroll Story Prototype

2D ink line-art scroll-driven animation: a character writes on papers, carries them to a board, and pins them. The marks (`^`, `<`, `_`, `>`) compose a triangle that fills yellow when complete.

Open `index.html` in a browser and scroll.
